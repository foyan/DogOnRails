<p id="notice"><%= notice %></p>

<ul>
<% @cart.line_items.each do |item| %>
	<li><%= item.quantity %> &times; <%= item.product.title %> = <%= item.total_price %></li>
<% end %>
</ul>
So viel: <%= @cart.total_price %>
<%= button_to 'Stop shopping', @cart, :method => :delete, :confirm => 'Are you sure?' %>