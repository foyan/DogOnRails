<% if line_item == @current_item %>
<li class="currentli">
<% else %>
<li>
<% end %>
	<%= line_item.quantity %> &times; <%= line_item.product.title %> = <%= number_to_currency(line_item.total_price) %></li>
